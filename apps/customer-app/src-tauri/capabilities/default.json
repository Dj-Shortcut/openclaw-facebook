{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default customer portal permissions. Keep native access minimal.",
  "windows": ["main"],
  "permissions": ["core:default", "deep-link:default"]
}
